# `no-implicit-defaults` 守卫

## 触发时机

当某个重要行为选择无法直接溯源到用户请求、仓库证据，或 `reframe` 中已写明的显式假设时，触发此守卫。

## 必需响应

- 暴露这个缺失的决策
- 将该决策退回到 `reframe` 或 `plan`
- 如果没有该决策就无法继续，则将本次运行标记为 `blocked`

## 阻断规则

不得用“合理默认值”或未声明偏好来填补空白。
