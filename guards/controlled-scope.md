# `controlled-scope` 守卫

## 触发时机

当当前变更开始拉入相邻清理、顺手重构、额外 ticket 或未计划行为变更时，触发此守卫。

## 必需响应

- 明确指出额外范围是什么
- 将其从当前 ticket 中移除，或者
- 如果它实际上是必需的，则返回 `ticket` 或 `plan`

## 阻断规则

不得以方便性、局部整洁，或“来都来了”为理由为范围增长辩护。
